# Dependencies

brew install tesseract ghostscript
pip install pytesseract Pillow PyMuPDF tkinterdnd2 pyinstaller

# Usage

Command-Line Usage
------------------

./ocr_compact.py --input file1.pdf file2.pdf --action both --output new

GUI Usage
---------

Create an app with pyinstaller

1. pyinstaller --windowed --onefile ocr_compact.py
2. cp dist/ocr_compact.app /Applications

